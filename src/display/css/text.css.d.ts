import { CSSInputValues } from '../../logic/models/inputs';
export declare const generateCSSText: ({ backgroundColor, containerSkewX, containerSkewY, hexagonsFirstRow, hexagonColor, hexagonGap, hexagonRotation, hexagonScale, hexagonSize, hexagonTransition, mediaQuery_1, mediaQuery_2, mediaQuery_3, textColor, }: CSSInputValues) => string;
